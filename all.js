let arr = [0, 1, 2, 3, 4, 5, 12]
function all(arr) {
	// Loop through every item in the array
	arr.forEach(item =>  { if ( item > 10) 
		
	{newItem = false}} )
	return newItem false
	// If the item evaluates to false, return false

	// If the loop ends, return 
	 return true
}
console.log(all(arr))
